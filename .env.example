# Copy to .env (or set in Netlify dashboard → Site settings → Environment variables)
# .env is gitignored — never commit secrets.

# Pattern's Bifrost LLM gateway API key.
# Required for the "Ask the data" chat feature.
# Get yours from the Pattern Bifrost team.
BIFROST_API_KEY=your-bifrost-key-here
